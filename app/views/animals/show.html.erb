<h1><%= @animal.name %></h1>

<%= link_to 'Edit', edit_person_animal_path(@person, @animal) %>
<%= link_to 'Delete', person_animal_path(@person, @animal), method: :delete, data: {confirm: 'You sure?'} %>
<hr />

<p>
	This persons age is: <strong><%= @animal.age %></strong>
</p>

<p>
	This persons gender is: <strong><%= @animal.species %></strong>
</p>

<h4>
	Is alive <%= @animal.alive ? 'Yes' : 'No' %>
</h4>
